# Price Announcer Local Agent Instructions

This directory is an experimental local utility area.

- Do not stage, commit, push, publish, deploy, or otherwise include files from this directory unless the user explicitly asks for this directory by name.
- Other project or page agents may ignore and skip this directory.
- Do not read files in this directory during unrelated work unless the user explicitly asks about `price_announcer/` or a file inside it.
- Changes in this directory are not part of the main site/page release flow by default.
- If a task explicitly targets this directory, follow the user's latest request and the repository root instructions.
